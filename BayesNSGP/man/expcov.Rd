% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/v03_NNGP_density.R
\name{expcov}
\alias{expcov}
\title{Exponential covariance function for Gaussian processes}
\usage{
expcov(edists, rho, sigma2)
}
\arguments{
\item{edists}{Numeric matrix (\eqn{M \times M}) of pairwise Euclidean distances between spatial locations.}

\item{rho}{Positive numeric scalar. Length-scale parameter \eqn{\rho} controlling the decay of correlation with distance.}

\item{sigma2}{Positive numeric scalar. Signal variance parameter \eqn{\sigma^2} controlling the overall variance.}
}
\value{
A numeric matrix (\eqn{M \times M}) representing the covariance between spatial locations.
}
\description{
Computes the exponential covariance matrix.
}
\details{
The covariance function is defined as:
\deqn{C_{i,j} = \sigma^2 \exp(-d_{i,j} / \rho)}
where \eqn{d_{ij}} is the distance between locations \eqn{i} and \eqn{j}.

Larger values of \eqn{\rho} imply slower decay of correlation with distance
(i.e., stronger spatial autocorrelation). Larger values of \eqn{\sigma^2}
increase the marginal variance of the process.

A small nugget term (1e-6) is added to the diagonal for numerical stability.
}
\examples{
coords <- matrix(runif(10), ncol = 2)
d <- as.matrix(dist(coords))
expcov(edists = d, rho = 0.5, sigma2 = 1)

}
\author{
Fabian Ketwaroo
}
